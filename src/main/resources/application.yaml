spring:
  servlet:
    multipart:
      max-file-size: 10MB      # 파일 하나 최대 크기
      max-request-size: 10MB   # 요청 전체 최대 크기
  application:
    name: rental

  datasource:
    # 1. 접속 주소에 allowPublicKeyRetrieval=true 필수! (RSA 보안 문제 해결)
    url: jdbc:mysql://localhost:3306/neorental_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 0902

  jpa:
    # 2. 최신 하이버네이트용 방언 명시
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

server:
  servlet:
    session:
      timeout: 60m  # 세션 초기화 시간을 60분으로 설정

jwt:
  secret: VlwEyVlPKZ6yV9yVlwEyVlPKZ6yV9yVlwEyVlPKZ6yV9yVlwEyVlPKZ6yV9y # 임의 설정

file:
  upload-dir: /Users/oppenheimer/2026/rentalservice_sprint/rental-images/

# 👇 [추가] 웹소켓 매핑 과정을 자세히 보기 위한 로그 설정
logging:
  level:
    org.springframework.messaging: DEBUG
    org.springframework.web.socket: DEBUG